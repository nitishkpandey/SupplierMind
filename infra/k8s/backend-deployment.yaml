apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: suppliermind
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: suppliermind-backend:latest
          imagePullPolicy: Never    # Use local minikube image
          envFrom:
            - secretRef:
              name: suppliermind-secrets
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: suppliermind
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
